PORT=3000

DATABASE_URL="postgresql://tsuser:tspass@localhost:5432/tsapp?schema=public"

REDIS_URL=redis://localhost:6379

JWT_SECRET=change-me-in-production

# Optional: HMAC-SHA256 webhook verification (X-Signature header)
# WEBHOOK_SECRET=change-me-in-production

# Optional: force gateway timeouts for retry testing
# FORCE_GATEWAY_TIMEOUT=true
